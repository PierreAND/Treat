nelmio_api_doc:
    documentation:
        info:
            title: Treat API
            description: API de l'application Treat
            version: 1.0.0
        components:
            securitySchemes:
                Bearer:
                    type: http
                    scheme: bearer
                    bearerFormat: JWT
        security:
            - Bearer: []
    areas:
        path_patterns:
            - ^/api